/** @type {import('next-sitemap').IConfig} */
// Default code you can customize according to your requirements.
module.exports = {
  siteUrl: process.env.SITE_URL || 'https://paperarium.place',
  generateRobotsTxt: true,
  generateIndexSitemap: false,
  // REST CODE READ DOCS  ...
};
